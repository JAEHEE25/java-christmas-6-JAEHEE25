package christmas.controller;

import christmas.domain.BenefitReport;
import christmas.domain.Order;
import christmas.domain.contants.BenefitReportInfo;

import java.util.Map;

public class BenefitReportController {
    /*
    private final BenefitReport benefitReport = new BenefitReport(BenefitReportInfo.createBenefitReport());

    public void setOrderMenuList(Order order) {
        benefitReport.putOrderMenuList(order);
    }

    public String getBenefitReport() {
        StringBuilder report = new StringBuilder();
        Map<String, String> benefitReportResult = benefitReport.getBenefitReport();

        for (String title : benefitReportResult.keySet()) {
            report.append(title).append("\n").append(benefitReportResult.get(title));
        }

        return report.toString();
    }

     */
}
